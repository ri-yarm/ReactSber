![Profiler](Profiler.png)

1) TaskCard после обёртки в memo карточки перестал перерисовываться при смене фильтра и удалении других задач

2) TaskList ререндерится при любом изменении состояния но ограничивается сранвением пропсов.

прошу проверить файл(пометил тудушкой) `TaskList` не избыточна ли обёртка для filterButton?


